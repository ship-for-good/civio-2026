# PostgreSQL configuration — copy to .env and customize locally
# Do NOT commit the .env file with real credentials.

POSTGRES_USER=civio
POSTGRES_PASSWORD=change-me-locally
POSTGRES_DB=civio
POSTGRES_PORT=5432
